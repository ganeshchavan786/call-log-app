<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.CallLog" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_600</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/purple_100</item>
        <item name="colorSecondary">@color/teal_700</item>
        <item name="colorTertiary">@color/color_incoming</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
    </style>

    <!-- Splash Screen theme -->
    <style name="Theme.CallLog.Splash" parent="Theme.CallLog">
        <item name="android:windowBackground">@color/purple_600</item>
    </style>

    <!-- Bottom Sheet Dialog styling -->
    <style name="ThemeOverlay_App_BottomSheetDialog" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Widget_App_BottomSheet_Modal</item>
    </style>
    <style name="Widget_App_BottomSheet_Modal" parent="Widget.Material3.BottomSheet.Modal">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearanceOverlay_App_BottomSheet</item>
    </style>
    <style name="ShapeAppearanceOverlay_App_BottomSheet" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">16dp</item>
        <item name="cornerSizeTopRight">16dp</item>
    </style>

</resources>
